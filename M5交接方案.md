# M5 开发交接方案：概率盲盒玩法 + 手机独立游玩

> 本文档写给"零上下文的执行者"（任何 AI 模型或开发者）。只依赖本文档与仓库内文件即可开工，无需本次对话历史。
> 交付两件事：A-C 概率与盲盒玩法（扭蛋机/闪光/每日礼盒），D-E 手机独立游玩（PWA + 部署）。

---

## 0. 如何使用本文档

1. 先读第 1-4 节（现状、约束、架构、美术管线），再按第 5-9 节逐任务实施
2. 每完成一个任务：跑测试（第 10 节）→ 浏览器截图目检 → 在 `游戏策划案.md` 末尾追加交付记录（仓库内有大量先例，照格式写）
3. 第 12 节"常见坑"是前期开发踩过的真实问题，动手前务必读完

## 1. 项目现状快照

- **游戏**：《水豚温泉町》，肥鹅健身房式合成经营（Merge 棋盘），单文件零依赖
- **位置**：本目录（`水豚温泉町/`）。关键文件：
  - `index.html` — 游戏全部代码（CSS + SVG 符号库 + DOM + 单个 script，约 2700 行）
  - `art/` — AI 生成贴图：`items/` 33 张食材、`chars/` 8 张角色与生成器、`scene/town.png` 场景、`raw/` 原始精灵表与备份、`slice.py` 切图、`cutout.py` 去背
  - `tests/headless.js` — 无头测试套件（84 项，自包含，直接 `node tests/headless.js`）
  - `游戏策划案.md` — 完整设计 + 历次交付记录（了解已有系统读这个）
  - 上级目录 `devserver.py` + `.claude/launch.json` — 开发服务器（禁缓存）
- **运行**：`cd 上级目录 && python3 devserver.py 8080 水豚温泉町`，浏览器开 `http://localhost:8080`
- **已有系统**（勿重复造）：合成链 5 条（veg/bun/dairy 基础 + cook1/cook2 烹饪成品）、生成器充能与离线回充、订单与客人好感雏形、剧情对话系统、主线目标链（QUESTS）、星星与装修 9 项（RENO）、泡泡开荒、商店（SHOP）、小礼盒宝箱（chest）、烹饪台配方发现（RECIPES）、储物间、图鉴、成就式提示、音效

## 2. 硬约束（违反即返工）

1. **全项目零 emoji**：源码、注释、UI 文案、toast、文档，一律不用 emoji。图形用 SVG 或 PNG 贴图
2. **存档向后兼容**：玩家已有存档（localStorage key `wonsen1`）。新增 S 字段必须在 `load()` 里给默认值；`QUESTS` 数组**只允许尾部追加**（quest.i 是索引，中间插入会错位）
3. **贴图优先 + 矢量回退**：物品/角色渲染必须走 `itemArt(id)` / `artUse(sym, cls, dir)`，它生成"PNG 叠 SVG"结构；SVG 默认 `display:none`，仅贴图 `onerror` 调 `artFail()` 打上 `.noimg` 后显示。**不要**改回"onload 后隐藏"方案——DOM 重建瞬间会闪出矢量底色（踩过）
4. **换贴图必须把 `ART_VER` +1**（index.html 内的资源版本号，绕浏览器缓存）
5. **中文 UI 文案**，语气与现有文案一致（治愈、口语、无感叹号堆砌）

## 3. 代码架构速览

- **数据表**（script 顶部，全部数据驱动）：`CHAINS`（含 cooked 标记）、`ITEM_NAMES`、`ITEMS`（含 `val` 计价字段：cooked 链 = tier+3）、`RECIPES`、`GENS`、`CUSTS`、`CUST_LINES`、`STORY_*`、`QUESTS`、`RENO`、`SHOP`
- **状态对象 `S`** 现有字段：`v, coins, xp, level, mute, tut, dex[], board[63], gens{gid:{ch,last}}, orders[3]{c,needs,rf}, sold, story{intro,merge,deliver}, met[], thx[], quest{i,p}, stat{deliver}, stars, reno[], store[4]`
- **存档**：`save()`/`load()`，棋盘序列化为稀疏数组 `[i, 't', id或gid]`；格子类型 `t`：`'i'` 物品 / `'g'` 生成器 / `'b'` 泡泡 / `'c'` 礼盒
- **棋盘**：7 列 x 9 行 = 63 格；`isOpen(i)` 由 `openRows()`（6 + 已购地板装修数）决定；`buildBoard()` 一次建格，`rCell(i)` 局部刷新
- **常用函数**：`toast(msg)` 堆叠提示、`showStory(lines, done)` 对话（`[{c:'capy'|客人id|null, t:'...'}]`）、`checkQuest(type)`、`sfx(kind, tier)`、`fxPop/fxMerge/floatText`、`artSrc(dir,name)`
- **调试接口** `window.__game`（测试全靠它）：以 index.html 尾部定义为准，先 grep `window.__game` 看全量。已知含 `S()`（活引用）、`tapGen`、`tryMove`、`deliver`、`canDeliver(i)`、`setOrder(slot,needs,cust)`、`spawn(id,near)`、`buyReno`、`buyShop`、`openChest`、`dlgOpen`、`dlgTap`、`addXp`、`regen`、`count`、`save`、`stopTick` 等。新增系统必须同步在此暴露测试入口

## 4. 美术管线

- **风格锚点**（生图提示词直接复用此段英文）：
  `Art style identical to cozy healing Chinese casual mobile game item icons: soft thick rounded warm-brown outlines, simple soft cel shading, bright pastel palette, chunky adorable kawaii shapes, uniform lighting, consistent style across all icons, plain light cream background between tiles. High quality, no text, no watermark`
- **生图工具**：codex CLI 的 grok-go MCP（`image_gen`，本机 127.0.0.1:8787）。产物在 `~/.grok-go/artifacts/`
- **切图**：`art/slice.py` 的 `slice_cards(src, names, cols, rows, out_dir, grid=None)`——按"卡底真实边界"切，保证边框完整、无多余余白。自动格线不准时（图标溢出格外、分隔线低对比）**直接显式传 `grid=(left, top, pitch_x, pitch_y)`**，不要给探测算法打补丁。预检可用 `SHOW_GRID=1 python3 slice.py <表> x,x,... 3 3 /tmp/check`
- **去背**：`art/cutout.py` 的 `cutout(src, dst, tol)`；tol 经验值：仅剥方形余白 26 不够，剥掉卡底圆盘需 42（角色有完整描边包围，泛洪进不去，白色皮毛安全）
- **本次已备好素材**：`art/raw/sheet_gacha.jpg`（3x3 扭蛋奖品表，已生成勿重复生图）。切图命令：
  ```python
  from slice import slice_cards
  slice_cards('raw/sheet_gacha.jpg',
      ['lantern','crane','pin','cat','geta','fan','duck','turtle','moon'],
      3, 3, out_dir='cards')   # 在 art/ 目录下执行；探测失准就补 grid 参数
  ```
  对应：石灯笼/纸鹤/小风车/招财猫/小木屐/团扇（R 卡）+ 金鸭子/温泉神龟/月见兔（SR 卡）

## 5. 任务 A：扭蛋机与卡牌册

**目标**：金币的娱乐性出口 + 盲盒期待感 + 收集长线（对标肥鹅健身房的盲盒/卡牌）。

- **数据**：
  ```js
  var CARDS = {
    lantern:{n:'石灯笼',r:'R'}, crane:{n:'纸鹤',r:'R'}, pin:{n:'小风车',r:'R'},
    cat:{n:'招财猫',r:'R'}, geta:{n:'小木屐',r:'R'}, fan:{n:'团扇',r:'R'},
    duck:{n:'金鸭子',r:'SR'}, turtle:{n:'温泉神龟',r:'SR'}, moon:{n:'月见兔',r:'SR'}
  };
  var GACHA_COST = 100;   // 金币；或消耗 1 张扭蛋券（券优先）
  ```
- **S 新增**：`cards:{}`（id→数量）、`tickets:0`。save/load 补默认值
- **概率**：N 62%、R 31%、SR 7%
  - N 安慰奖五五开：金币 40-90，或 2 份随机 1-3 级食材上盘（走闪光 roll，见任务 B；棋盘满则只给金币）
  - R/SR：随机对应卡池一张；**重复卡自动折金币**（R +60 / SR +200），卡册计数仍 +1（显示 xN）
- **UI**：
  - `#stations` 行加第三块瓷贴"扭蛋机"（新画 `i-capsule` 内联 SVG 图标：胶囊上下两半 + 中缝 + 星）；三块并排，窄屏字号自适应（现有两块的样式照抄）
  - 弹窗 `#gachaM`（复用 `.modal .panel` 样式）：顶部费用行（金币 100 / 券 xN）、大按钮"扭一次"、结果区（胶囊图形摇晃 0.6s 后揭示，CSS keyframes）、下方 3x3 卡牌册（未拥有 = 暗剪影 + "？？？"，拥有 = 卡面 + xN 角标；卡面用 `artUse('card-'+id, '', 'cards')` 需先给 9 张卡注册 SVG 兜底符号——可画极简占位符号，贴图为主）
  - 集卡奖励：6 张 R 集齐 toast +300 金币；3 张 SR 集齐 → 额外剧情一句（阿汤台词）+500 金币
- **调试**：`__game.gachaPull(forceRarity)`，`forceRarity` 可传 'N'|'R'|'SR' 供测试固定结果
- **主线**：QUESTS **尾部**追加一条：`{ t:'试试手气：扭一次扭蛋', type:'gacha', n:1, coins:30, story:[...] }`，`checkQuest('gacha')` 在 pull 后调用

## 6. 任务 B：闪光变体系统

**目标**：概率收集的狠肝层（策划案 v3 既定设计）。

- **掉落**：`rollShiny()` = 5% 概率。挂在四处：`tapGen` 产出、`buyShop` 食材包、`openChest` 食材、扭蛋 N 奖食材。泡泡开荒与烹饪产物暂不闪光（留 M6）
- **数据**：棋盘物品加可选标记 `{t:'i', id, s:1}`；**序列化**：cells 增加第 4 位 `[i,'i',id,1]`，`load()` 兼容旧 3 位格式（`c[3]` 不存在视为非闪光）
- **合成规则**：闪光只能与闪光合成（`(A.s?1:0)!==(B.s?1:0)` 时 bounce 并 toast "闪光食材只能和闪光的合成哦"）；产物继承闪光
- **渲染**：`rCell` 对闪光物品加 `.sh` 类并在 innerHTML 末尾附 `<span class="shOv"></span>`；CSS：金色内描边 + 两颗四角星角标 + 缓慢闪烁动画（复用 `#i-spark` 符号或纯 CSS）
- **图鉴**：`S.dexS = []`；首次合成出闪光物品 → 收录 + 奖励 `val*20` 金币 + toast；图鉴弹窗加"闪光图鉴"分区（仅列已收录，金框；未收录不显示防剧透）
- **交付**：`deliver` 挑选物品时**优先消耗闪光**，每份闪光额外 `+val*8` 金币；toast 追加"（闪光加成 +X）"
- **出售**：闪光价 `val*4`；信息卡名称显示"闪光·XX"
- **调试**：`__game.spawn(id, near, shiny)` 第三参可选

## 7. 任务 C：每日礼盒

**目标**：零惩罚的每日回访钩子（策划案 M4 项）。

- **数据**：`S.gift = {last:'YYYY-MM-DD', stock:1}`（新档默认 stock 1）；启动/读档时按本地日期差补货 `stock = min(3, stock + 相差天数)`，更新 last
- **UI**：场景左下角小礼盒按钮（absolute 定位进 `#scene`，参照 `#capyBtn` 写法；图标直接用 `art/items/chest.png` 的 `<img>` + 数量角标 `.chb`）；stock 0 时半透明
- **开启**：点击消耗 1 个 → 奖励随机：50% 金币 50-120 / 35% 两份随机 1-3 级食材（过闪光 roll，棋盘满则转金币）/ 15% 扭蛋券 +1；toast + fxPop。stock 0 → toast "礼盒明天再来，不急"
- **调试**：`__game.openGift()`、直接改 `S.gift` 测补货逻辑

## 8. 任务 D：PWA 化（手机可安装、离线可玩）

- 新增 `manifest.json`：name/short_name 水豚温泉町、`display:"standalone"`、`theme_color:"#C7E39A"`、`background_color:"#FDF6E5"`、icons 192/512
- 图标生成（PIL）：`art/chars/capy.png`（透明底）居中合成到奶油绿圆角方底上，导出 `icon-192.png`、`icon-512.png`
- 新增 `sw.js`：install 时 `cache.addAll`（index.html、manifest、icons、`art/items/*`、`art/chars/*`、`art/scene/*`——列表可由构建时枚举写死）；fetch cache-first；`CACHE_NAME` 含版本号，activate 清旧缓存
- `index.html` head 补：`<link rel="manifest">`、`<link rel="apple-touch-icon" href="icon-192.png">`、`<meta name="apple-mobile-web-app-capable" content="yes">`、`<meta name="theme-color" content="#C7E39A">`
- **注册守卫（重要）**：仅 `location.protocol==='https:'` 时注册 service worker。本地 localhost 开发**不注册**，否则 sw 缓存会让"改完刷新看不到"复发（此坑已为浏览器缓存修过一轮，别再引入）

## 9. 任务 E：部署（手机脱离电脑玩）

本机现状：`gh`/`wrangler`/`netlify` 均未安装。三个选项按省事程度排序：

- **选项 1（最快，免命令行）Netlify Drop**：
  1. 打包：`cd 上级目录 && zip -r 水豚温泉町.zip 水豚温泉町 -x "*/raw/*" "*/tests/*" "*.py" "*/M5交接方案.md" "*/游戏策划案.md"`（约 4MB，排除开发文件）
  2. 浏览器打开 app.netlify.com/drop（需免费账号），把 zip 拖进去，得到 `https://xxx.netlify.app` 链接
  3. 手机浏览器打开 → 分享菜单 → "添加到主屏幕"→ 图标像 App 一样出现，PWA 离线生效，之后不需要电脑
- **选项 2 GitHub Pages（利于后续自动更新）**：`brew install gh && gh auth login`（浏览器授权）→ `gh repo create` 推送 `水豚温泉町/` → 仓库 Settings 开 Pages。注意仓库为**公开**，介意就用选项 1
- **选项 3 只打包**：产出 zip 交用户自行处理
- **务必告知用户**：存档在手机浏览器 localStorage 里；清浏览器数据或换设备会丢档。"存档导出/导入串"列入 M6 待办

## 10. 测试要求

测试套件：`tests/headless.js`（自包含读取 `../index.html`，`node tests/headless.js` 运行，当前 84 项全过）。DOM 桩已就绪，新系统照现有断言风格**追加**（勿删旧断言）：

- 闪光：normal+shiny 合成 bounce；shiny+shiny 合成产物带 s；dexS 收录与奖励；出售价 val*4；交付含闪光加成的金币数值断言；存档 roundtrip 后 s 标记保留
- 扭蛋：金币不足拒绝；`gachaPull('R')` 扣 100 金币且 cards 计数 +1；重复卡折金币数值断言；`gachaPull('SR')`；券优先于金币消耗；cards/tickets 存档 roundtrip
- 礼盒：`S.gift.last` 设为昨天 → 补货 +1 且上限 3；开启后 stock -1 且奖励生效（金币变多或棋盘物品变多或券 +1）；stock 0 时 openGift 返回 false
- 主线：扭蛋后 `quest` 相应推进（若当前主线已过扭蛋节点则跳过该断言）

浏览器验证（Chrome 无头或有头均可）：扭蛋动画与卡册渲染截图、闪光物品金框截图、礼盒按钮角标截图；PWA 用 Lighthouse 或 Application 面板确认 manifest/sw 被识别（部署到 https 后验证）。

## 11. 验收清单

- [ ] 扭蛋机瓷贴入口 + 弹窗 + 摇晃揭示动画 + 卡牌册九宫（含未拥有剪影态）
- [ ] 9 张卡面从 `art/raw/sheet_gacha.jpg` 切出且边框完整（放绿色底上目检无白边/缺边）
- [ ] 重复卡折金币、集齐奖励触发
- [ ] 闪光五处掉落点生效、合成规则正确、金框视觉、闪光图鉴分区、交付加成、售价翻倍
- [ ] 每日礼盒按日期补货、三种奖励、上限 3
- [ ] 老存档载入不报错、不丢数据（拿改动前的 save JSON 手工回灌验证）
- [ ] `ART_VER` 已 +1；全文件零 emoji（用码点扫描，别肉眼）
- [ ] `node tests/headless.js` 全过（新增约 15 项）
- [ ] `游戏策划案.md` 追加 M5 交付记录（含根因/做法/验证三段式，照先例）
- [ ] manifest + sw + 图标齐备，https 下可"添加到主屏幕"并离线启动
- [ ] 给出手机游玩链接或 zip 与三步指引

## 12. 常见坑（全部真实踩过）

1. **浏览器缓存**：开发必须用 `devserver.py`（发 no-store），普通 `http.server` 会让改动"看似不生效"。sw 注册只放 https（见任务 D）
2. **无头 Chrome 最小视口 500px**：想截手机宽度，把页面包进 `<iframe style="width:390px">` 的外壳页再截
3. **并行会话**：用户可能同时开着别的会话改同一文件。**每次编辑前重新 grep 锚点**，用"唯一字符串断言替换"（python `assert old in s`）而非行号
4. **QUESTS 只能尾部追加**（quest.i 索引错位）；S 新字段 load 必须给默认
5. **棋盘序列化**：cells 是位置数组，闪光标记放第 4 位，读档兼容 3 位老格式
6. **精灵表切图**：先 `SHOW_GRID=1` 预检；表规整但探测失准时直接显式 `grid=`，别调启发式；切完放鲜绿底上目检边框
7. **贴图回退结构**：新增任何图标渲染都走 `artUse`/`itemArt`，勿手写 `<img>`（会漏 `artFail` 与版本号）
8. **验证习惯**：逻辑改动跑 headless 测试；视觉改动截图；数值断言精确到具体数字，模糊断言抓不住真 bug

---

## 附：后续排期提醒（本次不做）

M6 起：存档导出/导入串（手机换机）、流浪商人、专精树、梦境解谜关卡、客人好感等级化、四季框架。总蓝图见 `游戏策划案.md` 第五至十节。
